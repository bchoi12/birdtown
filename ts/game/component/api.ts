
export enum ComponentType {
	UNKNOWN,
	ASSOCIATION,
	ATTRIBUTES,
	CARDINALS,
	HEX_COLORS,
	MODEL,
	OPENINGS,
	PROFILE,
	STAT,
	STATS,
}

export enum AssociationType {
	UNKNOWN,

	OWNER,
	TEAM,
}

export enum AttributeType {
	UNKNOWN,

	BRAINED,
	GROUNDED,
	READY,
	SOLID,
}

export enum StatType {
	UNKNOWN,

	HEALTH,
}